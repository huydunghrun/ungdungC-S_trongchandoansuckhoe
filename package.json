{
  "name": "mindcare-app",
  "version": "1.0.0",
  "description": "Website khám bệnh tâm lý với AI OpenAI, video call, MongoDB",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && live-server --port=3000\"",
    "install-all": "npm install && cd backend && npm install"
  },
  "keywords": [
    "mental-health",
    "openai",
    "webrtc",
    "mongodb"
  ],
  "author": "MindCare Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "live-server": "^1.2.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.3"
  }
}
